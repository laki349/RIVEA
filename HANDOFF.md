# RIVEA — 인수인계 (다른 환경에서 이어서 작업할 때 이 파일부터)

> 최종 갱신: **2026-08-23** · 브랜치 `feat/outbound-catalog` · 배포 https://rivea-app.web.app
> 이 파일은 **지금 상태 + 왜 그렇게 정했는가 + 다음에 뭘 하나**만 담는다.
> 세부 근거는 `docs/`에 있고, 어느 문서를 언제 읽는지는 §8 문서 지도에 있다.
> `docs/05-project-status.md`는 **8/19까지의 이력**이다. 그 뒤 작업(8/20~23)은 이 파일에만 있다.

---

## 1. 한 줄로

연세 AX 비즈니스 랩 프로젝트. **40대+ 여성 홈케어 중개형 마켓플레이스 「RIVEA(리베아)」.**
Next.js 정적 export → Firebase Hosting. 지금 폰으로 열어볼 수 있는 상태다.
**목표 마감은 2026-09-02 피칭데이.** 그 전까지 필요한 건 코드가 아니라 **숫자와 관찰**이다.

| | |
|---|---|
| 로컬 경로 | `/Users/jinsihu/Desktop/workplace/mature care` |
| 배포 URL | https://rivea-app.web.app (Firebase 프로젝트 `rivea-app`) |
| 스택 | Next.js 14 App Router · TypeScript · Tailwind · Firebase(Auth 익명·Firestore·Hosting) |
| 렌더링 | **전체 정적 export** (`output: "export"`) — 서버 없음. 그래서 Spark(무료) 플랜으로 돈다 |
| 로컬 실행 | `npm run dev` (3000) |
| 계정 분리 | Claude는 **학교 계정**(`yonsei063@ormbiz.co.kr` · org `yonsei ax camp`), Firebase·GitHub는 **개인 계정**(`sihujin107@gmail.com` · `laki349`). **앱과 데이터는 학교 계정과 무관하다** |
| 상태 저장 | 대부분 `localStorage` + 계정 스코프(`src/lib/scope.ts`). 서버 저장은 leads·events뿐 |

---

## 2. 지금 상태 (2026-08-23 기준)

**작업 브랜치는 `feat/outbound-catalog`이고 `main`에 아직 머지하지 않았다.**

최근 흐름 (8/20 → 8/23)

| 커밋 | 무엇 |
|---|---|
| `9cfad5a` | 계측·고민 강화·실사진 필드 (아웃바운드 준비) |
| `806c204` | 계측을 실제 화면에 심음 + 성분 근거표 |
| `c2dd4c2` | 실제 제품 사진 17종 + 논문 근거를 화면에 |
| `5abfe4d` | **아웃바운드 링크 + 판정 카드 — 4단계 퍼널을 닫음** |
| `ff7d404`~`ab7653e` | QA 5건 수정 (아래 §5) + 본문 글자 전 구간 +2px |
| `10343c7` | 루틴 화면 마감 3건 — 저녁 안내문 고민별 분기 · 두피 제품 2개까지 · 빈자리 문장 반복 제거 |
| (8/25) | **창작 상품 8종·가상 브랜드 6곳 삭제 + 실제 제품 7종 추가 → 카탈로그가 전부 실제 제품이 됐다** |

**2026-08-25 — 카탈로그를 실제 제품만으로 정리했다**

지운 것: 상품 `p1`~`p8`(창작), 브랜드 라비드·오브제·셀렌·뮤엘·온휴·비타랩(가상 6곳).
없는 회사의 배송비·무료배송 기준을 화면에 띄우고 있었고, 처방이 그 제품들을 자리에 앉히고 있었다.

새로 수집한 것 (전부 공식몰, `pricedAt: 2026-08-25`):

| id | 제품 | 가격 | 메운 구멍 |
|---|---|---|---|
| `c-nutree-timezero` | 에버콜라겐 타임제로 30일분 | 24,500 | **이너뷰티 고민이 통째로 비었다** (p8이 유일했다) |
| `c-nutree-time-biotin` | 에버콜라겐 타임 비오틴 30일분 | 28,600 | 위와 같음 |
| `c-drforhair-tonic` | 폴리젠 씨크닝 스칼프 토닉 120ml | 19,900 | 두피가 샴푸 하나뿐이라 「샴푸+토닉 둘까지」 판단이 성립 못했다 |
| `c-drforhair-serum` | 폴리젠 씨크닝 스칼프 세럼 50ml | 29,900 | 위와 같음 |
| `c-anua-pdrn-serum` | PDRN 히알루론산 캡슐 100 세럼 30ml | 22,500 | 건조 세럼 자리 (설문 Q22가 아는 단어 두 개가 이름에 다 있다) |
| `c-anua-pdrn-cream` | PDRN 히알루론산 100 수분 크림 50ml | 21,000 | 레티놀을 그만두지 않게 덮는 자리 |
| `c-roundlab-vita-cream` | 비타 나이아신 잡티 크림 50ml | 24,000 | 색소 크림 자리 |

같이 손댄 것:
- 루틴 `r1`~`r4`를 실제 제품으로 다시 짰다. **id는 그대로 뒀다** — 매거진과 홈 히어로가 참조한다.
- `r11`(이너뷰티)·`r12`(자외선) 신설. 이 두 고민엔 루틴이 하나도 없었다.
- `c-laroche-uvmune`에 `wrinkle` 추가. 주름 고민에서 아침 차단 자리가 통째로 비어 있었다.
- **할인율 0%를 화면에서 감춘다.** 새 루틴은 세트가 = 단품 합계다(우리는 결제를 안 받으니
  깎아줄 수도 없다). 그러자 「0%」와 같은 숫자에 그은 취소선이 나왔다 — 렌더 조건을 `rate > 0`으로 바꿨다.
  ⚠️ **`r5`~`r10`에는 아직 10~15% 세트 할인이 남아 있다.** 우리가 결제를 받지 않는 이상
  줄 수 없는 할인이다. §5의 「상품 상세 CTA 3개」 판단과 같은 덩어리라 여기서 정하지 않았다.
- **남은 데모값: 평점·리뷰수·조회수(`cohortViews`)·결제혜택·적립.** 상품은 실제지만 이 숫자는
  우리 집계가 아니다. 홈 하단 고지에 이미 적혀 있고, 피칭에서 이 숫자를 근거로 쓰지 않는다.

**2026-08-25 (2차) — 「구매」가 판매처로 나간다. 로그인은 묻지 않는다**

운영자 결정. 전에는 「바로구매」가 장바구니에 담고 `/checkout`(회원 전용)으로 갔다.
우리는 결제를 받지 않으므로 그 체크아웃은 결제하는 척하는 화면이었다.

- `BuyBar`의 「바로구매」 → **`OutboundLink`(공식몰 구매)**. **로그인 게이트를 통과하지 않는다.**
  근거: 로그인의 값은 주문내역·배송조회·재구매인데 아웃바운드 구매는 그 셋 중 무엇도
  우리에게 남기지 않는다. 값이 0인 문턱을 세우면 잃는 건 **아웃바운드 클릭 = 입점 협상의
  유일한 숫자**다. **장바구니는 그대로 회원 전용** — 그건 다음에 다시 꺼내 보는 것이다.
- 나가는 문을 **컴포넌트 하나로 합쳤다**(`OutboundLink` + `variant`). 상품 상세 본문에 있던
  중복 아웃바운드 섹션은 지웠다. UTM 규칙과 `outbound_click` 호출이 갈라지면 집계가 반쪽이 된다.
- `ProductSource.buyUrl` 신설 — `sourceUrl`은 **가격을 확인한 곳**이라 목록·비교 페이지일 수 있다.
  구매 버튼이 목록으로 보내면 사람이 거기서 제품을 다시 찾아야 한다. 상세 페이지를 따로
  확인한 10건에 넣었다. 목적지 결정은 `buyUrlOf()` 한 곳에 있다.
- **상품 상세의 「결제혜택 최대 10% · 리베아 포인트 1% 적립 · 브랜드 배송비」를 지웠다.**
  셋 다 우리가 지어낸 값이고, 구매가 밖으로 나가는 순간 단순 데모값이 아니라 **거짓 안내**가 된다
  (우리 화면에서 적립을 약속하고 다른 가게로 보내는 꼴). 대신 결제·배송·가격이
  **어디서 정해지는지**와 가격 확인 시점만 남겼다.

**실제 제품 8종 추가 (2차 수집, 상품 32 → 40)** — 자리가 하나뿐이던 단계를 늘렸다.
각질 정리는 BHA 하나, 마스크팩도 하나뿐이라 「한 번 고르면 다음 선택지가 없는」 단계가 있었고,
이중세안 자리는 아예 비어 있었다.
라운드랩 독도 클렌징오일·필링젤·워터겔마스크 / 자작나무 톤업선크림 /
메디힐 티트리 마스크·토너패드 / 아누아 PDRN 미스트 / 뉴트리 에버콜라겐 타임레티놀A.

⚠️ **남은 것: 장바구니 → `/checkout`은 아직 결제하는 척한다.** 「구매」가 밖으로 나가는 지금
장바구니의 역할은 「나중에 살 것 모아두기」에 가깝다. 9/2 데모에서 결제 화면을 보여줄지는
여전히 제품 판단이다(§5 「판단 대기」와 같은 덩어리).

**2026-08-25 (3차) — 상품 40 → 61. 한 자리에 선택지를 둘 이상 만들었다**

수집처: 아누아(어성초·자작나무 6) · 라로슈포제(시카플라스트·선플루이드 2) ·
메디힐(PDRN 패드·세럼 3) · LG생활건강몰(글래스라이크 6 + 프라엘 기기 2) ·
닥터포헤어(씨크닝 샴푸 1) · 뉴트리(스킨앤헤어 1). 전부 공식몰 실측.

지금까지는 「이 단계엔 이것 하나」였다. 고민이 겹치는 사람에게 같은 제품이 아침·저녁·
양쪽 고민에 계속 나오면 처방이 한 장짜리로 보인다. 지금은 모공+건조를 고르면
아침 세안은 폼, 저녁 세안은 클렌징 오일로 갈린다.

⚠️ **상품이 늘자 드러난 결함을 하나 고쳤다 — `Regimen.sub` 신설.**
두피 제품이 샴푸 하나뿐일 땐 「자리 하나에 둘까지」로 충분했는데, 샴푸가 둘이 되자
처방이 **샴푸 두 개를 같이 쓰라고** 내밀었다. 「감는 것(wash)」과 「감고 남기는 것
(leave-in)」이 다른 역할인데 단계 이름이 `scalp` 하나라 구분할 수가 없었다.
이제 `prescribe`의 `extra`는 **역할이 겹치면 하나만** 고른다. 역할이 없는 단계
(이너뷰티)는 자연히 하나만 남는다 — 콜라겐 두 통을 같이 먹으라는 말도 틀렸다.
**앞으로 한 단계에 제품을 더 넣을 때는 「이 둘이 같이 쓰는 것인가, 둘 중 하나인가」를
먼저 답하고 `sub`를 준다.**

남은 공백: **기미커버(cover-makeup)는 아직 1종**이다. 아이오페 공식몰이 열리지 않아
못 늘렸다. 아이케어(눈가) 전용 제품도 아직 없다.

**2026-08-25 (4차) — 상품 61 → 91 · 브랜드 20. 눈가·목 단계를 새로 만들었다**

추가 수집: 토리든 22 · AHC 2 · 유세린 · 센텔리안24 · 헤라 · 클리오 · 에스트라 ·
메디큐브 넥크림. 신규 브랜드 7곳.

⚠️ **`STEP_ORDER`에 `eye`(눈가)와 `neck`(목·턱선)을 넣었다.** 넣기 전에는 아이크림·
넥크림이 **이름에 「크림」이 있다는 이유로 얼굴 마지막 자리를 차지했다** — 6만원짜리
목 전용 제품을 얼굴에 바르라는 처방이 된다. `stepOfSkincare`에서 **부위 판정이 제형
판정보다 먼저**다. 둘 다 `OPTIONAL`이라 채울 게 없으면 자리째 사라진다 — 아이크림을
안 쓰던 사람에게 빈 칸을 내밀면 없던 숙제가 하나 는다.

⚠️ **공식몰 주소를 확인 못 한 브랜드가 5곳이다**(AHC·유세린·센텔리안24·헤라·클리오·
에스트라). **지어내지 않고 비웠다.** `buyUrlOf`가 다나와 가격비교로 떨어뜨리고,
상품 상세 문구도 「공식몰」이 아니라 「가격비교에 연결된 판매처」로 바뀐다
(`viaOfficial`). 나중에 공식몰을 확인하면 `brands`에 `officialUrl`만 채우면 된다.

카테고리 분포: 스킨케어 42 · 클렌징 17 · 디바이스 10 · 마스크 7 · 선케어 4 ·
두피 4 · 이너 4 · 기미커버 3.
**남은 편중: 토리든 22종으로 한 브랜드가 전체의 1/4이다.** 라인 전체가 단품으로
공개돼 있어 수집이 쉬웠기 때문이고, 다음 수집은 다른 브랜드부터 채우는 게 맞다.

**2026-08-25 (5차) — 처방 로직 QA. 근거표는 통과, 카탈로그 연결이 문제다**

리포트: `.gstack/qa-reports/qa-report-rivea-logic-2026-08-25.md` (건강 점수 85). **아무것도 고치지 않았다.**

통과한 것 — `actives.ts` 인용 4건을 원문까지 확인했고 숫자가 논문과 일치했다:
Hughes 2013(PMID 23732711, 903명·4.5년·24%) **완전 일치** · Sci Rep 2025;15:26889
(RCT 23편·3,905명, 레티놀·글라이콜릭애씨드) **완전 일치** · PMC8459234(습진 RCT, caveat 정확) ·
PMID 10839713(500달톤). **등급 부여가 보수적이고 caveat가 불리한 사실을 숨기지 않는다.**

⚠️ **핵심 결함 — 근거표가 카탈로그에 연결돼 있지 않다.**
화장품 90종 중 **64종에 `actives`가 없다.** 그 64종은 (1) `findInteractions`가 `p.actives`로만
키를 모으므로 **병용 판정에 존재하지 않고**, (2) 고민 배정이 근거표가 아니라 **손으로 붙인
`concerns` 태그**로 결정된다. 처방 화면은 그 태그를 「· 주름·탄력」 같은 **이유 라벨**로
사용자에게 되돌려준다. 주름 단독 처방 10칸 중 근거가 있는 건 레티놀·선크림 **2칸뿐**인데
8칸에도 같은 라벨이 붙는다.

⚠️ **자기모순 1건** — `activeInfo.collagen`은 "바르는 형태의 실제 역할은 표면 보습막"이라고
적어놓고, 바르는 콜라겐 3종을 `wrinkle` 태그로 처방에 넣는다. 셋 다 `actives`가 없어서
**그 반박 근거 카드가 정확히 필요한 제품에서만 안 뜬다.**

⚠️ **인용 오류 1건** — 트라넥삼산 `caveat`의 「네트워크 메타분석에서 레이저·IPL보다 순위가
낮았다」는 PMID 38843906이 아니라 **다른 논문**(PMC12562867) 내용이다. 사실은 맞고 출처가 틀렸다.
나이아신아마이드 「4%가 2%보다」는 출처를 찾지 못했다(`docs/17 §3`에 PMID가 있으면 옮기면 끝).

**상품 91 → 100 (넘버즈인 9종, 브랜드 21곳).**
이번 회차부터 **`actives`를 채울 수 있는 제품을 우선 고른다.** 넣을수록 위 면적이 커지므로
최소한 새로 넣는 것은 늘리지 않는다. 판테놀 3종·자외선차단 2종·PHA 1종에 `actives`를 넣었고,
**글루타치온C 앰플·NMN PDRN 토너·로즈 PDRN 콜라겐 세럼은 일부러 건너뛰었다** —
`ActiveKey`에 없는 성분이라 넣으면 ISSUE-001과 똑같은 모양이 된다.

**2026-08-25 (6차) — QA 결함 닫음 + 상품 상세 정리**

**결함 (QA 리포트 ISSUE-001·002·004)**
- **이유 라벨은 근거가 있을 때만.** `prescribe.ts`에 `backedReason()` 신설 —
  제품의 `actives`가 `activeInfo`까지 이어지고, 그 성분이 다루는 고민이 이 자리의 고민과
  맞을 때만 「· 주름·탄력」을 붙인다. 아니면 `null`. **제품은 그대로 추천하되 이유를
  지어내지 않는다** — 자리를 비우면 세안·토너 같은 기본 단계가 통째로 사라지기 때문이다.
  주름 단독 처방에서 라벨 **8개 → 2개**(레티놀·선크림). QA가 지목한 그 2칸이다.
- **`sunscreen.concerns`에 `wrinkle` 추가.** 위 규칙을 넣자 선크림에서 라벨이 사라졌는데,
  인용한 Hughes 2013이 정확히 **광노화 시험**이다. 규칙이 너무 좁아 진짜 근거를 지운
  경우라 근거표 쪽을 고쳤다.
- **바르는 콜라겐 3종에서 `wrinkle` 제거.** `activeInfo.collagen`이 "바르는 형태의 실제
  역할은 표면 보습막"이라고 적어놓고 주름 자리에 놓는 건 자기모순이었다. 보습은 남겼다.
- **트라넥삼산 출처를 둘로 분리.** caveat의 「레이저·IPL 대비 순위」는 PMID 38843906이
  아니라 PMC12562867 내용이었다. 확인 못 한 「RCT 63편」은 뺐다.

⚠️ **ISSUE-003(병용 엔진 사각지대)은 아직 열려 있다.** `findInteractions`가 `p.actives`로만
키를 모으므로 `actives` 없는 64종은 여전히 판정에 안 잡힌다. 닫으려면 `ActiveKey`에
PDRN·센텔라·히알루론산을 추가하고 `docs/17`에 근거를 정리해야 한다(대부분 C등급이 나올 것).

**상품 상세 정리 — 근거를 지우지 않고 접었다**

실사용에서 「번잡하다」는 지적. 재보니 상세가 **2,906px(3.6화면)**이고 그중
「성분이 하는 일」 하나가 **942px(32%)**였다. 성분마다 등급·주장·단서·출처가 전부 같은
무게로 펼쳐져 성분이 셋이면 세 번 반복됐다. **읽을 게 많은 게 아니라 어디부터 읽을지가 없었다.**

- `ActiveBars`에 `<details>` 도입. 펼쳐둔 층 = 성분명·함량·역할·근거 등급(훑어서 비교되는 것),
  접은 층 = 주장·단서·출처. **JS 없이 동작**하므로 정적 export와 맞고 스크린리더가 기본 지원한다.
  `<summary>`는 40대+ 대상이라 **44px 터치 타깃**을 확보했다.
- **성분별 고민 태그 제거.** 성분마다 붙어서 같은 태그가 세 번 반복됐는데 페이지 위쪽
  해시태그 줄에 이미 있다. 같은 정보를 두 번 그리는 게 번잡함의 절반이었다.
- 결제·배송 두 행이 사실상 같은 말을 두 번 해서 한 행으로 합쳤다. 평점·조회수도 한 줄로.

결과: 성분 섹션 **942 → 453px**, 페이지 전체 **2,906 → 2,315px(-20%)**. 근거는 한 번 눌러 전부 열린다.

**2026-08-25 (7차) — 병용 엔진 닫음 + 계측을 실사용자용으로 굳힘**

인스타 링크로 실사용자를 받을 예정. 로직이 빈약하면 안 되고 트래픽이 조용히 0이 되면 안 된다.

**ISSUE-003 닫음 — `ActiveKey`에 `hyaluronic`·`centella`·`pdrn` 추가.**
없던 게 아니라 **우리가 등록을 안 한 성분**이라 40여 종이 통째로 판정 엔진 밖에 있었다.
`actives` 보유율 **28% → 67%**(화장품 90종 중 61종).
검증: 「레티놀과 비타민C는 시간대를 나눠서」가 이제 실제로 뜬다 — QA에서 「조용히 안 뜬다」고
지목한 바로 그 조합이다.

⚠️ **PDRN은 Grade C다.** 문헌을 열어보니 콜라겐(500달톤)과 같은 구조의 문제였다 —
분자량이 크고 음전하를 띠어 **일반 분자량 PDRN을 세럼으로 바르면 진피 섬유아세포까지
닿지 않는다**는 게 현재 정리다. 주사 형태는 9개 연구·219명에서 개선 보고가 있지만
근거의 질은 낮음~보통. 그래서 `pdrn.concerns`는 **`["dry"]`만** 준다 — 주름·모공을 붙이면
카탈로그의 PDRN 9종이 전부 근거 없는 주름 라벨을 달게 된다.
**같은 이유로 `collagen.concerns`에서도 `wrinkle`을 뺐다.**

⚠️ **오배선 3건 교정** — 마데카소사이드 제품을 `panthenol`로 붙여놨었다. 다른 성분이다.
(메디힐 마데카 2종 · 센텔리안24 아이크림 → `centella`)

**계측 — 인스타 유입에서 조용히 0이 되는 구멍 셋을 막았다**

파이프라인 자체는 살아 있다(실측: 이벤트 76건·세션 10개·아웃바운드 4건이 브랜드별·제품별로
집계됨). 문제는 **실패가 전부 조용하다**는 것이었다.

1. **`readSid()`가 sessionStorage를 직접 불렀다.** 저장소가 던지는 환경(사파리 프라이빗,
   일부 인앱 브라우저)에서 예외가 async try에 삼켜져 **모든 이벤트가 유실**된다.
   `safeSession` 래퍼를 만들어 막히면 메모리로 버틴다.
2. **`?ref=`가 세션에 안 남았다.** 정적 export라 링크를 누르면 주소에서 사라지고,
   집계 쪽 보정은 **첫 이벤트가 저장됐을 때만** 동작한다. 인증이 늦어 `app_open`이 유실되면
   그 세션의 유입이 통째로 direct가 됐다. 이제 읽는 순간 sessionStorage에 붙잡는다.
   **검증: `/?ref=insta`로 들어가 다른 페이지로 이동해도 `insta`가 유지되고, 집계에 1세션으로 잡힌다.**
3. **인증을 이벤트마다 병렬로 시도했다.** 진행 중인 약속 하나를 공유하게 바꿨고,
   실패 시 `console.warn`을 남긴다 — 개발자도구를 열면 바로 보인다.

**인스타 링크는 `?ref=` 를 붙여 뿌린다:** `https://rivea-app.web.app/?ref=insta`
(스토리·프로필·게시물을 가르려면 `insta_story` 처럼 값만 바꾼다. 40자까지.)

**2026-08-25 (8차) — 로그인을 입구에서 떼고 「돌아올 이유」 자리로 옮겼다**

실사용자를 인스타로 받기 전 마지막 정리. 운영자가 「로그인 필수로 갈까」를 물었고,
**입구는 열고 판정 자리에서 받는 쪽**으로 정했다.

무엇이 문제였나: 첫 진입 시퀀스가 **스플래시 → 소개 → 로그인 패널**이었고,
`/my-routine`·`/shelf`가 `OPEN_PREFIXES`에 없어서 **처방을 보러 온 사람도 같은 문**을 만났다.
이 앱이 파는 게 정확히 그 두 화면인데 그 앞에 문이 있었다.

로그인을 입구에서 받지 않는 이유 셋:
1. **증명이 안 된다.** 익명 uid가 이미 고유 사용자를 센다. 피칭에서 강한 숫자는 가입자 수가
   아니라 퍼널이다 — 「몇 명이 처방까지 갔고 몇 명이 공식몰로 나갔나」. 콜드 트래픽의
   가입 수는 작을 수밖에 없고 그건 보여주기 **더 나쁜** 숫자다.
2. **설문이 반대를 말한다.** 「고를 때 불편」 1위가 "이게 나한테 맞는 건지 모르겠다" 50%
   (`docs/15`)인데, 그 답 앞에 벽을 세우는 셈이다.
3. **잃은 걸 셀 수 없다.** 벽에서 나간 사람은 `app_open`조차 남기지 않는다.

바꾼 것:
- `Onboarding`에서 **로그인 패널 제거** → 2패널(스플래시·소개) 뒤 게스트로 자동 진입
- **`?ref=`가 붙은 방문은 온보딩 자체를 건너뛴다.** 목적이 분명한 방문이라 브랜드 소개가
  낄 이유가 없다. 인스타 링크는 이제 **바로 홈**이다
- `OPEN_PREFIXES`에 `/my-routine`·`/shelf` 추가
- **`KeepPrompt` 신설** — 화장대에 무언가 등록한 게스트에게만, 화장대 화면에서 권한다.
  "N개를 등록하셨어요. 14일 뒤에 판정해드릴게요 / 지금 이 기록은 이 브라우저에만 있어요."
  겁을 주지 않는다(「지워집니다」 아님). 닫으면 이 브라우저에서 다시 뜨지 않는다.

**원칙: 로그인은 사용자에게 값이 생긴 다음에 묻는다.** 돌아올 이유가 없는 사람에게 계정을
권하면 우리 편의를 파는 것이고, 있는 사람에게 권하면 그 사람의 기록을 지켜주는 것이다.

검증: localStorage·sessionStorage를 전부 비운 신규 방문자가 `/?ref=insta`로 들어가면
**스플래시 없이 홈**, `/my-routine`도 로그인 문구 0건. 화장대에 1개 등록하면 권유가 뜨고
「괜찮아요」를 누르면 다시 뜨지 않으며 화장대는 유지된다.

**2026-08-25 (9차) — 적합성 축 신설. 「이게 나한테 맞나」에 답한다**

운영자 지적: **고민과 적합성은 다른 얘기다.** 고민은 「뭘 해결할까」고 적합성은 「내 피부가
견딜 만한가」인데, 두 축이 직교하는데도 처방은 고민만 봤다. 설문 「고를 때 불편」 1위가
「이게 나한테 맞는 건지 모르겠다」 16/32(50%)인데 그 문장의 '맞는'이 정확히 적합성 쪽이다.
(전에 `youIf`가 그 답이라고 적어둔 건 틀렸다 — `youIf`는 「이 고민이 당신 얘기인가」다.)

**타입을 묻지 않는다.** 「건성/지성/복합성」은 안 맞는 사람을 만든다 — 40대+에 흔한
「당기면서 번들거림」이 4분법 어디에도 없고, 이 프로젝트는 강제 단일선택으로 이미 데였다
(설문 72%). 대신 **관찰 문장 5개를 복수로 받고, 타입 이름으로 합치지 않는다.**

| 고른 문장 | 조정 |
|---|---|
| 세수하고 조금 지나면 당겨요 | 덮는 제형 ↑ |
| 오후엔 이마·코가 번들거려요 | 가벼운 제형 ↑ |
| 뭘 발라도 각질이 일어나요 | 덮는 제형 ↑ · 자극 ↓ |
| 새 제품을 쓰면 따갑거나 붉어질 때가 있어요 | 자극 ↓↓ · 진정 ↑ |
| 트러블이 잘 나요 | 가벼운 제형 ↑ |

⚠️ **새 제품 데이터를 만들지 않았다.** 「건성용」 태그를 손으로 붙이면 QA ISSUE-001이 그대로
재발한다. 그래서 **파생만 쓴다**(`src/lib/fit.ts`) — 제형은 **제품명**에서(젤·미스트·앰플 vs
크림·밤·오일), 자극/진정은 **`actives`**에서. 이 프로젝트의 「파생할 수 있는 건 저장하지
않는다」 원칙 그대로다. 판정 안 되면 **중립**이다.

⚠️ **당김+번들거림이 상쇄되던 버그를 잡았다.** 처음엔 rich와 light를 서로 빼도록 짰는데,
그러면 둘 다 고른 사람이 **아무것도 안 고른 것과 같아졌다** — 안 맞는 사람을 없애려고 만든
기능이 그 사람을 다시 버리는 꼴이었다. 두 축을 다른 수단에 배정한다: 번들거림은 **제형**으로
(가벼운 쪽), 당김은 **성분**으로(판테놀·히알루론산·세라마이드·센텔라 가점).

**적합성 점수는 한 자리 수**로 둔다. 고민 점수가 100 단위라, 피부 신호가 고민을 이기면
기미를 고른 사람에게 기미와 무관한 제품이 나간다. 이건 「무엇을 살까」가 아니라
「같은 자리에 후보가 여럿일 때 어느 쪽인가」다.

**화면은 처방 아래에 둔다**(`FitPanel`). 고민 다음에 바로 물으면 관문이 하나 더 생기고,
그건 이번에 입구에서 로그인을 걷어낸 것과 같은 실수다. **값을 먼저 보여주고 입력을 받는다.**
아무것도 안 고르면 처방은 이 기능이 없을 때와 정확히 같다.

**자극을 낮추면 근거가 두꺼운 성분이 뒤로 밀린다는 걸 화면에서 말한다.** 「따갑다」를 고르면
레티놀(근거 A)이 빠지고 PDRN(근거 C)이 들어온다. 그게 교환이라는 사실을 숨기면 유리한
절반만 보여주는 게 된다 — 「주 1~2회부터 시작하는 방법도 있어요」를 같이 낸다.

검증: 주름 고민 + 「따갑다」 → 저녁 세럼 레티놀 → PDRN, 눈가 유세린 → 센텔리안24(센텔라).
건조 고민 + 「번들거림」 → 크림 시카플라스트 → 하이드로세라 젤크림. 당김+번들 동시 → 젤크림 유지.

**커밋 안 된 작업(working tree)**

- `cardnews/` — `style.md` **§9 정보 밀도 규칙 신설**(6항의 「최소한만」을 정보형 회차에서 뒤집음),
  `CLAUDE.md` 해당 항목 폐기 표시, 회차 `260829_성분표3줄` 재작업.

→ 이어서 작업할 사람은 **이 덩어리를 먼저 커밋 또는 정리**하고 시작한다.

**⚠️ 원격 백업이 비어 있다.** `origin/main`(github.com/laki349/RIVEA)은 8/22의 `init` 커밋에서 멈춰 있고,
**로컬에만 63개 커밋(7/28~8/24)이 있다.** 이 노트북이 유일한 사본이다. 다른 환경으로 옮기기 전에
`git push`부터 한다.

---

## 3. 이 프로젝트에서 이미 확정된 판단 (되풀이해서 논쟁하지 말 것)

검증 순서 규칙: **설문 n=32(`docs/15`) → 대면 n=6(`docs/12`) → 문헌(`docs/07`).**
큰 표본이 이기되, **40대에 관한 것만은 대면이 이긴다.**

**살아남은 것**
- 진입은 **고민**이다 — 1순위 88%, 강제 단일선택 72%. concern-first IA는 데이터로 지지된다.
- 이탈은 **효과**다 — 「효과가 있는 건지 없는 건지 모르겠다」 **72%**, 방치·폐기 56%.
  이게 A′(효과 판정 루프)의 근거이자 앱의 중심 축이다.
- 커머스 신규 기능 요청은 「딱히 없다」 41%. → **결제를 우리가 받지 않고 공식몰로 내보낸다**(아웃바운드).

**죽은 것 — 근거 없음. 피칭 방어선으로 쓰지 않는다**
1. 「순서·병용 판정이 대체 불가능한 자산」 — 기기 보유자의 순서 혼란 리프트 **0** (`docs/15` §5).
   `rules.ts`는 처방 안쪽에서 계속 돌지만 **진입점·피칭 근거로 쓰지 않는다.**
2. 「캡쳐해놓고 못 찾는다」가 화장대의 진입 동기 — 1/32(3%). 단 **40대 셀은 미판별**(`docs/15` §1-2).

**설계 원칙 (코드 곳곳의 판단 기준)**
- **파생할 수 있는 건 저장하지 않는다.** 체크포인트(14·28일)는 시작일에서 파생하고,
  화장대는 주문에서 파생하고, 등급은 주문에서 파생한다. **사용자가 만든 답만 저장한다.**
- **모르겠다는 답을 반드시 남긴다.** 없으면 모르는 사람이 아무거나 눌러 데이터가 거짓이 된다.
- **홈에 배너를 늘리지 않는다.** 판정은 「슬롯」이다 — 답할 게 있을 때만 나타나고 답하면 사라진다.
- **40대+ 대상**이므로 본문 글자·터치 타깃을 줄이는 방향의 변경은 하지 않는다(전 구간 +2px 이력).
- **카탈로그에 없는 제품을 만들지 않는다 (2026-08-25 확정).** `source` 없는 상품은
  `catalog.ts`에 넣지 않는다. 처방은 「이걸 사서 이렇게 쓰세요」라는 말이고,
  퍼널의 끝은 공식몰로 내보내는 것이다 — 나갈 곳이 없는 상품은 그 두 가지를 동시에 거짓으로 만든다.
  새 상품은 공식몰·다나와에서 실측 수집하고 `source`(sourceUrl·pricedAt·priceNote)를 남긴다.

---

## 4. 앱 구조 지도

```
src/
├── app/          라우트 (정적 export 105 페이지)
├── components/   화면 부품 (VerdictCard·VerdictSlot·OutboundLink·ActiveBars 등)
├── data/
│   ├── catalog.ts     고민 7 · 브랜드 21 · 카테고리 8 · 상품 100(**전부 실측**) · 루틴세트 12
│   ├── actives.ts     성분 정보 + 논문 근거(evidence). 3종(알부틴·펩타이드·판테놀) 미작성
│   ├── regimen.ts     제품 → 단계(세안·토너·세럼·크림·선크림·두피·이너)
│   ├── interactions.ts 성분 병용 안내
│   └── magazine.ts    매거진 기사
└── lib/
    ├── prescribe.ts   고민 → 아침·저녁·주간·고민전용 처방
    ├── verdict.ts     14·28일 체크포인트 파생 + 판정 답 저장
    ├── shelf.ts       내 화장대(쓰고 있는 것) + 시작 시점
    ├── events.ts      계측 (아래 §5)
    ├── rules.ts       순서·병용 규칙 엔진
    ├── orders/cart/wish/reviews/repurchase/wallet/profile/recent/leads
    └── scope.ts       계정별 localStorage 키 + 게스트 승계
```

**핵심 화면 흐름 (4단계 퍼널과 같다)**

```
고민 고르기(/, /concern/[slug])
   → 처방 보기(/my-routine)
   → 화장대에 등록(/shelf, 「언제부터 썼나」 3지선다)
   → 14·28일 판정 카드(홈 슬롯 + 화장대 안)
   → 공식몰로 나가기(OutboundLink, 결제는 우리가 받지 않음)
```

---

## 5. 계측 — 9/2 피칭의 실탄

`src/lib/events.ts` · Firestore `events` 컬렉션 · **익명 인증 + create만 허용**(`firestore.rules`).
**개인정보 0필드**: 익명 uid · 이벤트명 · 값 하나 · 세션id · ref · 시각까지만.

| 이벤트 | 뜻 |
|---|---|
| `app_open` | 세션 시작 = **퍼널의 분모** (세션당 1건, sessionStorage로 보장) |
| `concern_select` → `prescription_view` → `shelf_add` → `verdict_answer` | 4단계 퍼널 |
| `outbound_click` | 공식몰 이동 = **제조사 입점 협상 재료** |
| `product_view` | 부수 지표 |

- 이벤트 이름을 **아무 데서나 지어내지 않는다.** `EventName` 타입에 없는 문자열을 쓰면 집계가 갈라진다.
- ⚠️ 배포할 때 `--only hosting`만 하면 **규칙이 안 올라간다.** 규칙을 고쳤으면
  `--only firestore:rules`를 같이 배포한다.
- 8/23 라이브에서 한 세션 안에 4단계 전 구간이 실제로 기록되는 것을 확인했다
  (`.gstack/qa-reports/qa-report-rivea-app-2026-08-23.md` 하단).

**아웃바운드를 저쪽도 알아보게 한다 (2026-08-24)**
`OutboundLink`가 링크에 `utm_source=rivea&utm_medium=referral&utm_campaign=product_detail&utm_content=<제품id>`를
붙인다. 우리 `outbound_click`만 있으면 협상 자리에서 그건 「우리 주장」이지만, UTM이 붙으면
같은 방문이 **브랜드 자기 애널리틱스에 `rivea / referral`로 잡혀** 저쪽 데이터로 검증된다.
uid·sid는 넣지 않는다 — URL은 저쪽 서버 로그에 남으므로 우리 DB와 성격이 다르다.
이미 `utm_source`가 있는 링크(저쪽이 준 제휴 링크)는 덮어쓰지 않는다.

**집계는 스크립트로 꺼낸다**

```bash
node docs/fetch-events.mjs --days=7
```

4단계 퍼널 도달률(세션 기준) · 아웃바운드 클릭을 **브랜드별·제품별**로 · 유입 경로를 출력한다.
규칙이 `events`의 read를 막아뒀으므로 **서비스 계정 키**가 필요하다(발급법은 스크립트 상단 주석).
키는 `service-account.json`으로 루트에 두고 **절대 커밋하지 않는다**(`.gitignore` 등록됨).

**8/23 QA에서 잡은 것 (전부 수정·검증 완료, 건강 점수 85 → 99)**
1. `app_open`이 페이지마다 찍혀 **분모가 거짓**이었다 → sessionStorage로 이동 (`eefbacf`)
2. 목록에서 고른 제품에 시작 시점을 안 물어 **실험 기간 내 판정이 영영 0**이었다 (`cf2aca4`)
3. 28일에 답하면 14일 카드가 뒤따라 떴다 (`ff7d404`)
4. 본문 글자가 대상 연령대에 작았다 → 12~24px 구간 +2px, 67개 파일 (`42f1a0d`)
5. 두피·이너뷰티 고민이 처방에서 통째로 무시됐다 → `CONCERN_STEPS` 신설 (`ab7653e`)

**이월(버그 아님, 콘텐츠 공백)** — 실사진 8종 미확보 · 성분 근거 3종 미작성.
**판단 대기** — 상품 상세에 CTA가 셋이다(공식몰 + 장바구니 + 바로구매). 결제를 안 받기로 했는데
장바구니가 남아 있다. **9/2 데모에서 결제를 보여줄지 아웃바운드만 남길지는 제품 판단**이라 QA가 정하지 않았다.

---

## 6. 카드뉴스 라인 (앱과 분리)

`cardnews/`는 인스타 매거진 제작 라인이다. **앱 코드와 무관하고 `package.json`도 별도**(Playwright).
회차 18개. 규칙은 `cardnews/CLAUDE.md`, 디자인 단일 기준은 `cardnews/style.md`.

절대 규칙 몇 개만 옮기면:
- 브랜딩은 **앞뒤 표지에만**. 본문 카드에 브랜드·구매 링크를 넣지 않는다.
- **이미지 생성 AI로 카드를 만들지 않는다.** HTML/CSS + Playwright 캡처.
- 사진은 **Unsplash / Pexels만.** 타 브랜드 로고가 읽히면 크롭으로 제외.
- 만들고 나면 `measure.mjs`·`veilcheck.mjs` 통과 필수, PNG는 컨택트 시트로 눈으로 확인.
- **2026-08-23 변경**: 정보형 회차는 「카드는 최소한만」을 뒤집는다. **카드가 자족해야 저장된다**(`style.md` §9).
  길어지면 문장을 줄이지 말고 **카드를 쪼갠다.**

---

## 7. 재배포 · 로컬

```bash
cd "/Users/jinsihu/Desktop/workplace/mature care"
npm run build
npx -y firebase-tools@latest deploy --only hosting --project rivea-app
```

규칙까지 바꿨다면:

```bash
npx -y firebase-tools@latest deploy --only hosting,firestore:rules --project rivea-app
```

**겪은 삽질 (반복하지 말 것)**
- `npm run dev`가 켜진 채로 `npm run build`를 돌리면 dev가 쓰던 청크를 지운다.
  증상은 `Cannot find module './vendor-chunks/next.js'` + **CSS가 전부 빠진 화면**. 코드 결함이 아니다.
- `npm run build`를 빼먹으면 **이전 빌드가 배포된다.**
- MCP `firebase_deploy` 도구는 "no site name" 에러를 낸다 → CLI 직접 실행이 안정적.
- `firebase_init`을 다시 돌리면 `out/`을 Firebase 웰컴 템플릿으로 덮어쓴다 → 다시 빌드하면 해결.

---

## 8. 문서 지도 — 무엇을 언제 읽나

| 파일 | 언제 |
|---|---|
| **이 파일 (`HANDOFF.md`)** | 항상 먼저 |
| `docs/05-project-status.md` | 8/19까지의 상세 이력 · 화면별 구현 메모 · 초기 결함 목록 |
| `docs/16-roadmap-0902.md` | **9/2까지 무엇을 어떤 순서로** — 트랙 A(전화)/B(외출)/C(코딩), 날짜별 표, 안 하는 것 |
| `docs/15-survey-findings.md` | **설문 n=32 판정표.** 가설이 죽고 산 근거. 숫자를 인용할 땐 여기서 |
| `docs/12-interview-findings.md` | 대면 n=6. 40대 관련은 여기가 우선 |
| `docs/07-validation-findings.md` | 문헌 + n=1. 가장 약한 근거 |
| `docs/17-ingredient-evidence.md` | **성분 근거표(A~R 등급).** 숫자를 화면·카드뉴스에 쓰기 전 반드시 확인 |
| `docs/01-foundation.md` | 사업 기획 원본 |
| `docs/02·03·04·06` | 와이어프레임 · 디자인 시스템 · 사진 브리프 · 매거진 표지 브리프 |
| `docs/08-cardnews-research.md` | 카드뉴스 방법론 근거 |
| `docs/09·11` | 인터뷰 스크립트 · 제조사 통화 스크립트 |
| `docs/10-keyword-findings.md` | 검색 키워드 조사 |
| `docs/13·14` | 설문 v2 설계 · 카테고리 네이밍 |
| `docs/ir/` | IR 덱(html·pptx) + **국표원 공급자 컨택리스트 66곳** |
| `.gstack/qa-reports/` | 최근 QA 리포트 + 스크린샷 |

---

## 9. 남은 일 (9/2까지)

`docs/16` 기준으로, 코딩(트랙 C)은 거의 끝났고 **남은 건 대부분 앉아서 하는 코딩이 아니다.**

**해야 하는 것**
1. 🚶 **무개입 관찰 3명 이상** (미용실·문화센터. 말 걸지 않는다 — 안내하면 관찰이 아니라 데모다)
2. 🚶 **40대 설문 8명** 추가 (누적 12명). 50대는 더 받아도 88%·72%가 안 바뀐다
3. 📞 제조사 통화 — ★4 개발비·손익분기 숫자가 **3.5억 요청 전체의 근거**다
4. 💻 **덱 5장** — 04 판정표 / 12 The Ask 재배치 / 05→12 연결 / 관찰 1장 / 4단계 도달률 1장
5. 💻 성분 근거 3종(알부틴·펩타이드·판테놀) · 실사진 8종 — 콘텐츠 공백 메우기
6. 💻 상품 상세 CTA 3개 문제 결정 (결제를 데모에 남길 것인가)

**안 하는 것 (14일 · 1인)**
콜드메일 대량 발송 · 입점 계약 · 사업자등록 · 커머스 신규 기능 · 저장·재발견을 첫 화면으로 승격 ·
병용·순서를 진입점이나 피칭 방어선으로 쓰는 것 · 사용법 영상 · `/gift` 승격(9/2 이후).

**피칭에서의 태도**: 가설 두 개가 죽은 걸 숨기지 않는다.
**회수 전에 기준을 정해놓고 자기 가설을 죽인 기록이 이 프로젝트에서 가장 강한 카드다.**

---

## 10. 새 환경에서 시작하는 법

```
"RIVEA 프로젝트 이어서 할게. HANDOFF.md 읽어줘."
```

그다음 필요에 따라: 숫자를 다루면 `docs/15`, 일정을 다루면 `docs/16`,
성분 문구를 쓰면 `docs/17`, 카드뉴스면 `cardnews/CLAUDE.md` + `cardnews/style.md`.
